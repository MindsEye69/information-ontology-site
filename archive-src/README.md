# information-ontology-site
Information Ontology 


## Archive snapshot export

This folder is preserved as the historical source. To publish it under `/archive`, export it (static) and copy the output into `../site/public/archive`.

Run from repo root:

```bash
./scripts/build-archive.sh
```
