This folder hosts the frozen historical snapshot of the prior site under /archive.

To (re)build the snapshot:
1) Build a static export from /archive-src with basePath "/archive".
2) Copy the export output into this folder, replacing its contents.

See /scripts/build-archive.sh in the repo root.
