import { authStatus } from "@/lib/admin/auth";
export const dynamic = "force-dynamic";
export default function AdminLoginPage({ searchParams }: { searchParams?: { error?: string; loggedOut?: string; next?: string } }) {
  const status = authStatus(); const error = searchParams?.error;
  return <div className="mx-auto max-w-md px-4 py-16"><p className="text-xs uppercase tracking-[0.2em] text-black/55">Administrator</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Sign in</h1><p className="mt-3 text-sm text-black/60 leading-relaxed">Use the administrator credentials configured in the deployment environment.</p>{!status.configured ? <div className="mt-6 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-900">Admin login is not configured. Set ADMIN_USERNAME and either ADMIN_PASSWORD_HASH or ADMIN_PASSWORD before using the admin panel.</div> : null}{error === "invalid" ? <div className="mt-6 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-900">Invalid username or password.</div> : null}{error === "not-configured" ? <div className="mt-6 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-900">Admin authentication is not configured yet.</div> : null}{searchParams?.loggedOut ? <div className="mt-6 rounded-2xl border border-black/10 bg-white/70 p-4 text-sm text-black/70">You have been logged out.</div> : null}<form action="/api/admin/login" method="post" className="mt-8 space-y-5 rounded-2xl border border-black/10 bg-white/70 p-6"><input type="hidden" name="next" value={searchParams?.next ?? "/admin"} /><label className="block"><span className="text-xs uppercase tracking-[0.16em] text-black/50">Username</span><input name="username" autoComplete="username" defaultValue={status.username} className="mt-2 w-full rounded-xl border border-black/10 bg-white px-4 py-3 text-sm outline-none focus:border-black/35" /></label><label className="block"><span className="text-xs uppercase tracking-[0.16em] text-black/50">Password</span><input name="password" type="password" autoComplete="current-password" className="mt-2 w-full rounded-xl border border-black/10 bg-white px-4 py-3 text-sm outline-none focus:border-black/35" /></label><button disabled={!status.configured} className="w-full rounded-2xl bg-black px-5 py-3 text-sm font-medium text-white hover:bg-black/90 disabled:cursor-not-allowed disabled:bg-black/30">Sign in</button></form></div>;
}
