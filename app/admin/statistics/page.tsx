import { AdminNav } from "@/components/admin/admin-nav";
import { StatCard } from "@/components/admin/stat-card";
import { requireAdminSession } from "@/lib/admin/auth";
import { aggregateAnalytics, getAnalyticsEvents } from "@/lib/admin/storage";
export const dynamic = "force-dynamic";
function Table({ title, rows, empty = "No data yet." }: { title: string; rows: { name: string; count: number }[]; empty?: string }) { return <section className="rounded-2xl border border-black/10 bg-white/70 p-6"><h2 className="text-lg font-semibold">{title}</h2>{rows.length ? <div className="mt-4 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left text-black/45"><th className="border-b border-black/10 py-2 pr-4 font-medium">Item</th><th className="border-b border-black/10 py-2 text-right font-medium">Count</th></tr></thead><tbody>{rows.map((row) => <tr key={row.name}><td className="border-b border-black/5 py-2 pr-4 text-black/70">{row.name || "Direct / none"}</td><td className="border-b border-black/5 py-2 text-right tabular-nums">{row.count}</td></tr>)}</tbody></table></div> : <p className="mt-4 text-sm text-black/55">{empty}</p>}</section>; }
export default function AdminStatisticsPage() { const session = requireAdminSession(); const metrics = aggregateAnalytics(getAnalyticsEvents()); return <div><AdminNav username={session.username} /><main className="mx-auto max-w-6xl px-4 py-10"><p className="text-xs uppercase tracking-[0.2em] text-black/55">Admin</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Site statistics</h1><p className="mt-3 text-sm text-black/60 leading-relaxed">Privacy-conscious aggregate analytics. IPs are stored only as salted hashes; admin and API routes are excluded from page-view tracking.</p><section className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4"><StatCard label="Total events" value={metrics.totalEvents} /><StatCard label="Page views" value={metrics.pageViews} /><StatCard label="PDF views" value={metrics.pdfViews} /><StatCard label="Downloads" value={metrics.downloads} /></section><section className="mt-8 grid gap-4 md:grid-cols-3"><StatCard label="24 hours" value={metrics.last24h} /><StatCard label="7 days" value={metrics.last7d} /><StatCard label="30 days" value={metrics.last30d} /></section><div className="mt-8 grid gap-3 md:gap-6 lg:grid-cols-2"><Table title="Top pages" rows={metrics.topPages} /><Table title="Top referrers" rows={metrics.topReferrers} /><Table title="Top PDF views" rows={metrics.topPdfViews} /><Table title="Top downloads" rows={metrics.topDownloads} /></div></main></div>; }
