import { AdminNav } from "@/components/admin/admin-nav";
import { authStatus, requireAdminSession } from "@/lib/admin/auth";
import { getAuditEvents } from "@/lib/admin/storage";
export const dynamic = "force-dynamic";
export default function AdminSecurityPage() { const session = requireAdminSession(); const status = authStatus(); const audit = getAuditEvents(200).reverse(); return <div><AdminNav username={session.username} /><main className="mx-auto max-w-6xl px-4 py-10"><p className="text-xs uppercase tracking-[0.2em] text-black/55">Admin</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Security & audit log</h1><section className="mt-8 grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-black/10 bg-white/70 p-6"><h2 className="text-lg font-semibold">Authentication</h2><dl className="mt-4 space-y-3 text-sm"><div className="flex justify-between gap-4"><dt className="text-black/55">Configured</dt><dd>{status.configured ? "Yes" : "No"}</dd></div><div className="flex justify-between gap-4"><dt className="text-black/55">Username</dt><dd>{status.username}</dd></div><div className="flex justify-between gap-4"><dt className="text-black/55">Password hash</dt><dd>{status.hasPasswordHash ? "Set" : "Not set"}</dd></div><div className="flex justify-between gap-4"><dt className="text-black/55">Plain password env</dt><dd>{status.hasPlainPasswordEnv ? "Set" : "Not set"}</dd></div><div className="flex justify-between gap-4"><dt className="text-black/55">Custom session secret</dt><dd>{status.hasCustomSecret ? "Set" : "Not set"}</dd></div></dl></div><div className="rounded-2xl border border-amber-200 bg-amber-50 p-6 text-sm text-amber-950 leading-relaxed"><h2 className="text-lg font-semibold">Recommended production settings</h2><p className="mt-3">Use ADMIN_PASSWORD_HASH instead of ADMIN_PASSWORD, set a long AUTH_SECRET, deploy over HTTPS, and keep /admin unlinked from public navigation.</p><p className="mt-3">Generate a password hash with: <code className="rounded bg-white/70 px-1.5 py-0.5">npm run admin:hash -- "your-password"</code></p></div></section><section className="mt-8 rounded-2xl border border-black/10 bg-white/70 p-6"><h2 className="text-lg font-semibold">Recent audit events</h2>{audit.length ? <div className="mt-4 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left text-black/45"><th className="border-b border-black/10 py-2 pr-4 font-medium">Time</th><th className="border-b border-black/10 py-2 pr-4 font-medium">Actor</th><th className="border-b border-black/10 py-2 pr-4 font-medium">Action</th><th className="border-b border-black/10 py-2 font-medium">Target</th></tr></thead><tbody>{audit.map((event, index) => <tr key={`${event.ts}-${index}`}><td className="border-b border-black/5 py-2 pr-4 text-black/55 whitespace-nowrap">{event.ts}</td><td className="border-b border-black/5 py-2 pr-4">{event.actor}</td><td className="border-b border-black/5 py-2 pr-4">{event.action}</td><td className="border-b border-black/5 py-2 text-black/55">{event.target ?? "—"}</td></tr>)}</tbody></table></div> : <p className="mt-4 text-sm text-black/55">No audit events yet.</p>}</section></main></div>; }
